# Language setting
language: node_js

# Version number
node_js:
  - 0.12

env:
 - test="latest"
 - test="masterryt"

git:
   submodules: false
   
build:
  pre_ci:
    - docker build -t=shiphitchcock4/hitchcock4_sample_nodenewimage1:test .
    
   
  pre_ci_boot:
    image_name: drydock/u14nodall
    image_tag: master
    pull: true
    options: '--privileged=true --net=bridge -e FOO=true -e BOO=false'
    
  ci:
    - ps -eax
    - ls -al

  post_ci:
   # - docker build -t=shiphitchcock4/hitchcock4_sample_nodenewimage1:test .
    - docker push shiphitchcock4/hitchcock4_sample_nodenewimage1:test1
    
integrations:
  hub:
 #Docker integration
    - integrationName: ship-docker
      type: docker 
