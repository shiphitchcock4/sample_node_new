# Language setting
language: node_js

# Version number
node_js:
  - 0.12

env: 
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:
  
  #pre_ci:
   # - docker build -t=shippabledocker/shiphitchcock1_sample_node .

  #pre_ci_boot:
   # image_name: shippabledocker/shiphitchcock1_sample_node
  #  image_tag: latest
  #  pull: false
   # options: '--privil   - shippable_cache_containereged=true --net=bridge -e FOO=true -e BOO=false'

  ci: 
    - ps -eax
    #- shippable_cache_container
    - ls -al
    #- sleep 9m
    
  cache: true 
  cache_dir_list: 
    - $SHIPPABLE_BUILD_DIR/node_modules

  #post_ci:
   # - docker push shippabledocker/shiphitchcock1_sample_node
